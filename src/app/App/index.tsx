import { Layout } from '../Layout';
import styles from './styles.module.scss';

export const App = () => (
  <div className={styles.wrapper}>
    <Layout>
      Wow-talent-calc
    </Layout>
  </div>
);
